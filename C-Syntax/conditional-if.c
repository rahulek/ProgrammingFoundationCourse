#include <stdio.h>

int main(void)
{
    int age = 74;

    // Conditional Logic with if
    if (age > 60)
    {
        printf("Age: %d is a senior citizen.\n", age);
    }
    // NOTE: No else condition here

    return 0;
}