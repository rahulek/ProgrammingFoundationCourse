#include <stdio.h>

int main(void)
{
    int age = 7;

    // Conditional Logic with if
    if (age > 60)
    {
        printf("Age: %d is a senior citizen.\n", age);
    }
    else
    {
        printf("Age: %d is NOT a senior citizen\n", age);
    }

    return 0;
}